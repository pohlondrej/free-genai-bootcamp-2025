name: example-plugin

services:
  example:
    build:
      context: ./plugins/example
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    depends_on:
      - nginx
    profiles:
      - example